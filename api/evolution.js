/**
 * Alias de rota para o mesmo handler de evolution-info.js (útil se algum deploy
 * não resolver o nome com hífen). Mantém uma única implementação.
 */
export { default } from './evolution-info.js'
